# SOCIAL MEDIA WEB APP

# TECHNICAL CONSTITUTION

Version: 2.2
Last Updated: 15 March 2026
Project Owner: Tuan Sidiq

This constitution governs all technical decisions,
tooling usage,
implementation patterns,
folder structure,
state boundaries,
styling discipline,
and architecture rules in this project.

Any deviation from this document is invalid
unless this constitution is revised first.

---

# 0. PROJECT PURPOSE

This project is a modern social media web application
built with professional engineering standards.

The purpose of this document is not to define engineering philosophy in general.

Engineering philosophy,
AI reasoning discipline,
and quality judgement belong to:

- AI_ENGINEERING_CONSTITUTION.md

Domain ownership and system-level bounded contexts belong to:

- SYSTEM_DOMAIN_ARCHITECTURE.md

State ownership and state-category discipline belong to:

- STATE_ARCHITECTURE.md

This document defines the technical implementation law of the project.

Its purpose is to ensure that the frontend system is built with:

- stable architecture
- clear technical boundaries
- predictable data flow
- scalable feature structure
- maintainable implementation discipline
- responsive visual integrity
- design-token compliance
- API-contract compliance
- state ownership discipline
- domain-boundary compliance

The goal is not merely to make features work.

The goal is to build a frontend system with sound architecture,
stable behavior,
clear boundaries,
and long-term maintainability.

---

# 1. RULE PRIORITY

In case of conflict, the following priority order applies:

1. TECHNICAL_CONSTITUTION.md
2. Approved Project-Level Architecture Decisions
3. AI_ENGINEERING_CONSTITUTION.md
4. SYSTEM_DOMAIN_ARCHITECTURE.md
5. STATE_ARCHITECTURE.md
6. API_INTEGRATION_CONTRACT.md
7. Approved Design Specification / Figma
8. DESIGN_SYSTEM_FOUNDATIONS.md
9. PROJECT_ROADMAP_CONSTITUTION.md
10. Session-Level Implementation Instructions
11. Temporary implementation assumptions

Important:

- No implementation may override this constitution.
- If any conflict is detected between this constitution and another governing document,
  the conflict must be reported immediately.
- Until the conflict is resolved,
  no assumption-based implementation is allowed.

---

# 2. PROJECT SCOPE

This project is a social media web application.

## 2.1 System Capability Scope

The current backend capability surface includes the following major domains:

- authentication
- self profile
- public profile
- explore posts
- timeline feed
- post creation
- post detail
- comments
- likes
- follow system
- saved/bookmarked content
- user search
- followers/following relationship lists

These domains represent what the system can support technically
based on the official API contract.

## 2.2 Phase 1 Frontend Scope

Frontend implementation must distinguish between:

- full backend capability
- currently active frontend phase

Phase 1 frontend implementation is restricted by the official roadmap.

Phase 1 frontend focuses on:

- login
- register
- self profile
- public user profile
- timeline feed
- explore posts
- create post
- post detail
- comment system
- user search

The following domains may exist in the backend
but are NOT mandatory for Phase 1 frontend interaction flows:

- likes interaction UI
- follow interaction UI
- saved/bookmark management UI
- followers/following management UI
- notifications
- settings
- messaging
- stories
- groups
- advanced recommendation systems

No technical implementation may silently expand scope
beyond the officially active roadmap phase.

---

# 3. LOCKED CORE STACK

The following tools are officially locked for this project:

- Next.js (App Router)
- TypeScript
- Tailwind CSS
- shadcn/ui
- Redux Toolkit
- TanStack Query
- Framer Motion
- Axios
- React Hook Form
- Zod
- Lucide React
- clsx
- tailwind-merge
- dayjs

Restrictions:

- No additional state library without constitutional approval
- No additional UI framework without constitutional approval
- No styling system outside the approved stack
- No replacement of the locked core stack without revising this constitution first

---

# 4. ARCHITECTURE PRINCIPLES

All implementation must follow these principles.

## 4.1 Predictability First

Code must be easy to trace,
reason about,
and debug.

## 4.2 Single Responsibility

Each module,
component,
hook,
and function must have one clear responsibility.

## 4.3 Separation of Concerns

UI,
server state,
client state,
validation,
data fetching,
and business logic must not be mixed carelessly.

## 4.4 Reusability with Discipline

Reusable code must emerge from real repeated need,
not from premature abstraction.

## 4.5 Feature-Oriented Scalability

The codebase must support feature growth
without collapsing into shared-folder chaos.

## 4.6 Maintainability Over Cleverness

Readable,
stable,
explicit code is preferred
over compact but obscure code.

## 4.7 No Hidden Coupling

No feature may silently depend on unstable assumptions
from another feature.

---

# 5. NEXT.JS APP ROUTER RULES

## 5.1 App Router is mandatory

This project must use Next.js App Router.

## 5.2 Default to Server Components

All components must be treated as Server Components by default
unless a Client Component is actually required.

## 5.3 Client Components are allowed only when necessary

Use `"use client"` only when the component needs one or more of the following:

- browser-only APIs
- local interactive state
- event handlers
- TanStack Query hooks
- Redux hooks
- React Hook Form behavior
- Framer Motion client behavior
- other client-only libraries

## 5.4 Minimize client boundary spread

Do not promote large subtrees to Client Components
unless truly necessary.

## 5.5 No unnecessary client wrappers

Do not wrap static presentational content
in Client Components
without clear technical need.

---

# 6. TYPESCRIPT LAW

## 6.1 Strict typing is mandatory

TypeScript must be used in strict mode.

## 6.2 No `any`

`any` is prohibited unless there is a formally justified exceptional case.

Allowed alternatives:

- `unknown`
- generics
- properly inferred types
- explicit interfaces/types
- zod inference

## 6.3 Real contract alignment

Types must reflect real backend/API contracts,
not imaginary idealized shapes.

## 6.4 Type duplication should be minimized

When a schema or DTO shape already defines the contract,
prefer deriving types rather than rewriting identical manual types.

## 6.5 Prefer explicit return types where clarity matters

Return types should be explicit when the function is:

- non-trivial
- shared
- exported
- critical for readability

---

# 7. STATE MANAGEMENT BOUNDARIES

State ownership is strictly divided.

## 7.1 Redux Toolkit = client UI state only

Redux Toolkit may be used only for client-side UI state, for example:

- modal visibility
- sidebar state
- drawer state
- selected tab
- local workflow coordination state
- filter UI state
- ephemeral interface control state

Redux Toolkit must NOT be used for:

- API response caching
- fetched lists
- remote entities
- mutation payload persistence
- duplicated server resource storage

## 7.2 TanStack Query = server state only

TanStack Query must handle:

- data fetching
- caching
- background refetching
- invalidation
- pagination / infinite queries
- mutation coordination with server-backed data

## 7.3 React local state = local component interaction

Use React local state only for isolated UI behavior
that does not require app-level sharing.

## 7.4 React Hook Form = form state

Forms must use React Hook Form for form state
and Zod for validation.

## 7.5 State meaning must remain explicit

Every stateful decision must answer clearly:

- who owns the state
- why the state exists
- who consumes it
- when it resets
- whether it is local,
  shared UI,
  form state,
  or server-backed

For deeper state ownership law,
the project must also obey:

- STATE_ARCHITECTURE.md

---

# 8. DATA FLOW LAW

Approved server data flow:

Axios Client
→ Service / API Layer
→ Query Hook / Mutation Hook
→ UI Component

Rules:

- Components must not call remote APIs directly
- Components must not manually transform raw backend contracts inline
  if that transformation belongs to service/query layer
- Query hooks must expose clean,
  predictable outputs
- Business-level mapping must be centralized,
  not repeated across UI files

Forbidden patterns:

- fetch/axios directly inside random components
- ad hoc API logic inside JSX/TSX route files
- duplicating server transformation logic in multiple components
- storing fetched server entities in Redux

---

# 9. AXIOS / API LAYER RULES

## 9.1 Centralized client

All HTTP requests must go through the approved Axios client.

## 9.2 No raw scattered API calls

No feature may instantiate its own ad hoc HTTP layer
without architectural reason.

## 9.3 API contracts and DTOs

Raw backend response shapes must be treated as DTOs
when appropriate.

Rules:

- DTO types may remain close to service/API layer
- UI components should consume cleaned domain/view shapes
  when raw DTOs are unsuitable
- Mapping from DTO → Domain Model must happen in service/query layer,
  not inside presentational JSX

## 9.4 No silent response assumptions

Every endpoint integration must follow the official backend contract exactly.

If the contract is unclear,
implementation must pause until clarified.

## 9.5 API contract authority

The official source of truth for integration details is:

- API_INTEGRATION_CONTRACT.md

This includes:

- endpoint path
- method
- auth requirement
- request shape
- response envelope
- pagination format
- field naming conventions

---

# 10. QUERY RULES

## 10.1 TanStack Query ownership

All server state hooks must be implemented with TanStack Query.

## 10.2 Query keys must be consistent

Query keys must be centralized through query key factories.

Approved pattern examples:

- `feedKeys.all`
- `feedKeys.list(params)`
- `postKeys.all`
- `postKeys.detail(postId)`
- `commentKeys.list(postId)`
- `profileKeys.me()`
- `profileKeys.public(username)`
- `searchKeys.users(params)`

## 10.3 No inconsistent string scattering

Do not hardcode unrelated random query keys across files.

## 10.4 Query hooks should be feature-scoped

Query hooks should live near the feature they belong to
unless they are intentionally shared across multiple domains.

## 10.5 Mutation handling must be explicit

Each mutation must define:

- success handling
- error handling
- invalidation/refetch behavior
- optimistic update strategy if applicable
- rollback strategy if optimistic update is used

## 10.6 Endpoint meaning must affect cache separation

The following must not share cache meaning carelessly:

- explore posts vs timeline feed
- self profile vs public user profile
- my posts vs public user posts
- my liked posts vs public user liked posts
- saved posts vs liked posts

---

# 11. FORM RULES

## 11.1 React Hook Form + Zod are the standard

Forms must use:

- React Hook Form for form state
- Zod for schema validation

## 11.2 Validation schema placement

Rules:

- feature-specific form schemas belong inside that feature
- shared reusable schemas may live in shared schema folders
  only if reused by multiple features

## 11.3 Prefer schema-derived typing

Prefer `z.infer<typeof schema>` when appropriate
to avoid duplicated type declarations.

## 11.4 Form UI and submission logic must be cleanly separated

Form rendering,
validation,
submission,
and post-submit handling
must not become tangled
inside one monolithic component.

## 11.5 Multipart forms must obey API contract

Any form involving file upload must follow the exact API contract.

Examples:

- profile update form must use the backend-approved multipart fields
- create post form must use the backend-approved image field name

No form may invent upload payload structure.

---

# 12. FOLDER STRUCTURE LAW

The codebase must follow a clear and scalable structure.

Approved top-level source structure:

- `src/app`
- `src/components`
- `src/features`
- `src/lib`
- `src/services`
- `src/store`
- `src/types`
- `src/constants`
- `src/config`

Detailed meaning:

## 12.1 `src/app`

Contains:

- routes
- route segments
- layout files
- page files
- route-level loading/error boundaries

Must NOT become a dumping ground
for unrelated business logic.

## 12.2 `src/components`

Contains shared components only.

Examples:

- layout components
- shared UI wrappers
- cross-feature presentational components
- global providers if needed

Sub-rules:

- reusable primitive UI components belong in `src/components/ui`
- shared layout components belong in `src/components/layout`
- truly shared display components may live in other clear subfolders

## 12.3 `src/features`

Contains feature-oriented modules.

Each feature may contain only what belongs to that feature.

Recommended feature substructure:

- `components`
- `hooks`
- `services` or `api`
- `schemas`
- `types`
- `utils`
- `constants`

A feature should own its local logic
as much as possible before escalating to shared folders.

## 12.4 `src/lib`

Contains cross-cutting technical utilities.

Examples:

- helper functions
- formatters
- utility functions
- shared technical adapters
- shared schema helpers
- shared query helpers

## 12.5 `src/services`

Contains global service-level infrastructure
when it is not feature-local.

Examples:

- axios instance
- interceptors
- global API helpers

## 12.6 `src/store`

Contains Redux store setup,
slices,
selectors,
and store-related shared utilities.

## 12.7 `src/types`

Contains truly shared global types only.

Do not move feature-local types here
just because the folder exists.

## 12.8 `src/constants`

Contains truly shared constants only.

Do not pollute this folder with feature-local constants
that belong inside a feature.

## 12.9 `src/config`

Contains project-level configuration surfaces
that do not belong elsewhere.

Examples:

- environment helpers
- runtime config wrappers
- app-level static config

---

# 13. PLACEMENT RULES

To remove ambiguity,
these rules are locked.

## 13.1 Shared vs feature-local

If a module is used by only one feature,
it must stay inside that feature
unless there is a strong shared need.

## 13.2 No fake reusability

Do not move code into shared folders prematurely.

## 13.3 UI primitives

Reusable primitive UI components must live in `src/components/ui`.

## 13.4 Feature forms

Feature forms and their schemas should stay inside their feature
unless intentionally shared.

## 13.5 Global services

Axios instance,
interceptors,
and global request infrastructure
belong in shared service infrastructure,
not inside unrelated features.

## 13.6 App routes are entry points, not logic warehouses

Route files should compose feature modules.
They must not become the primary home
for data orchestration and business logic.

---

# 14. STYLING LAW

## 14.1 Styling stack is locked

Only approved styling tools may be used:

- Tailwind CSS
- design tokens from the official project source
- shadcn/ui component styling conventions

## 14.2 Design token authority

Design tokens must originate from the official design system source.

No parallel unofficial token system may be introduced.

## 14.3 No raw random styling in component files

Forbidden unless explicitly justified:

- raw hex color literals in TSX/JSX
- ad hoc rgb/hsl values in component files
- arbitrary one-off values without design justification
- inline styles used as shortcuts for normal styling

## 14.4 Arbitrary Tailwind values are restricted

Arbitrary values such as `w-[37px]` or `top-[13px]`
may only be used when:

- the design truly requires it
- there is no suitable token or utility class
- the value is not better represented as a reusable token

## 14.5 Semantic styling is preferred

Prefer semantic design-system classes
over literal styling when such classes exist.

## 14.6 Token implementation must follow the global token system

All styling tokens must be implemented through the official global token layer.

Approved implementation location example:

- `src/app/globals.css`

Preferred token naming scheme:

- `--color-*`
- `--space-*`
- `--radius-*`
- `--font-*`

Component files must consume semantic utilities such as:

- `bg-background`
- `text-foreground`
- `border-border`

instead of raw token values or raw colors.

## 14.7 CSS UNIT POLICY

This project follows a rem-based layout scaling system.

Purpose:

- responsive consistency
- accessibility compatibility
- predictable scaling across devices
- alignment with professional frontend standards

### 14.7.1 Primary layout units

All layout-related sizing must use rem-based scaling where appropriate.

Examples include:

- font-size
- padding
- margin
- gap
- width
- height
- min-height
- max-width
- spacing inside components

Preferred examples:

- `h-12`
- `p-4`
- `gap-3`
- `w-80`
- `max-w-md`

These Tailwind utilities are rem-based and must be preferred over arbitrary pixel sizing.

### 14.7.2 Forbidden arbitrary pixel layout

Hardcoded pixel sizing for layout must be avoided.

Discouraged / forbidden by default:

- `w-[320px]`
- `h-[48px]`
- `p-[12px]`
- `m-[14px]`

Pixel-based layout breaks scalable design rhythm
and should not be used unless clearly required by design
and justified explicitly.

### 14.7.3 Allowed pixel usage

Pixel units are allowed only for fine visual details such as:

- borders
- hairline separators
- SVG stroke width
- minor visual offsets when unavoidable

Examples:

- `border`
- `border-b`
- `border-[1px]`

### 14.7.4 Tailwind scale preference

Developers must prefer official Tailwind scale utilities first,
then semantic token utilities,
before considering arbitrary values.

Priority:

1. Semantic utility
2. Tailwind scale utility
3. Arbitrary value only if truly necessary

### 14.7.5 Accessibility impact

Rem-based scaling helps the interface respect:

- browser font scaling
- accessibility zoom
- system scaling behavior

This rule is mandatory,
not optional polish.

---

# 15. FLUID RESPONSIVE SCALING POLICY

Modern responsive design in this project must not rely solely on breakpoints.

Certain layout elements must scale fluidly across screen sizes to preserve
visual rhythm and proportional balance between mobile, tablet, and desktop.

To achieve this, the CSS `clamp()` function is adopted as an official scaling tool.

This policy defines when and how clamp must be used.

---

## 15.1 PURPOSE

The purpose of clamp usage is to create:

- fluid scaling between viewport sizes
- smoother transitions between breakpoints
- proportional typography and spacing
- improved visual consistency across devices

Clamp enables controlled scaling with defined minimum and maximum bounds.

---

## 15.2 BASIC STRUCTURE

Clamp follows the structure:

`clamp(minimum, preferred scaling, maximum)`

Example:

`font-size: clamp(1rem, 2vw, 1.5rem);`

Meaning:

- minimum size: `1rem`
- fluid scaling based on viewport: `2vw`
- maximum size: `1.5rem`

This ensures the element never becomes too small or excessively large.

---

## 15.3 REQUIRED USAGE AREAS

Clamp must be used for elements that require fluid responsive scaling
between mobile and desktop when fixed breakpoint jumps would produce
stiff or visually unbalanced results.

Clamp must be considered mandatory when responsive scaling is needed for the following areas:

### Typography

Large typography must scale fluidly across devices.

Examples:

- hero titles
- section titles
- display headings
- prominent marketing copy
- major empty-state headlines

Example usage:

`font-size: clamp(1.5rem, 3vw, 2.5rem);`

### Container spacing

Major layout spacing may use clamp to avoid extreme padding differences.

Examples:

- section padding
- layout margins
- container side spacing
- major block spacing
- hero vertical rhythm

Example usage:

`padding-inline: clamp(1rem, 4vw, 3rem);`

### Layout container width

Fluid container sizing may use clamp to maintain optimal reading width
or adaptive centered content width.

Examples:

- hero content wrappers
- auth content wrappers when required by design
- central content shells
- large card containers
- marketing content bounds

Example usage:

`max-width: clamp(20rem, 90vw, 60rem);`

---

## 15.4 WHEN CLAMP MUST NOT BE USED

Clamp must not be used blindly for every size.

Avoid clamp for:

- small UI element spacing
- icon sizes
- border radius
- minor component gaps
- precise alignment adjustments
- one-off decorative offsets
- small control dimensions that should remain stable

Using clamp excessively can make layout behavior unpredictable
and harder to debug.

---

## 15.5 DESIGN SYSTEM COMPATIBILITY

Clamp values must still respect the design system tokens.

Clamp boundaries must reference existing token scales whenever possible.

Example:

`font-size: clamp(var(--font-lg), 3vw, var(--font-2xl));`

This ensures clamp does not break the design system.

If token references are not yet available for a required clamp use case,
rem-based values may be used temporarily,
but they must remain deliberate and documented.

---

## 15.6 RESPONSIVE STRATEGY

The responsive strategy of this project follows a hybrid approach:

1. Layout structure controlled by breakpoints
2. Typography and major spacing controlled by clamp where fluid scaling is needed
3. Token-based spacing preserved through the design system
4. Tailwind rem-based scale remains the default baseline for standard component sizing

This approach ensures:

- predictable layout
- fluid scaling
- maintainable codebase
- accessible scaling behavior
- consistent desktop-to-mobile responsiveness

---

## 15.7 IMPLEMENTATION PRIORITY

Clamp is not a replacement for all responsive techniques.

Implementation priority must follow this order:

1. Correct layout structure
2. Correct breakpoint behavior
3. Rem-based sizing and Tailwind scale
4. Clamp for fluid scaling where required
5. Arbitrary values only if genuinely justified

Clamp must improve the layout,
not compensate for weak structure or poor spacing logic.

---

# 16. UI SOURCE OF TRUTH

Visual implementation authority is defined as follows:

1. Approved Figma / design specification governs visual intent
2. DESIGN_SYSTEM_FOUNDATIONS.md governs official design tokens
3. This constitution governs engineering method and implementation boundaries
4. Session instructions may clarify details but cannot override the above

Rules:

- UI must respect design intent
- responsive behavior must preserve design meaning,
  not invent a new design
- if design specification and architecture requirement conflict,
  the conflict must be reported before implementation
- existing implementation is NOT automatically the source of truth
  if it contradicts approved design

---

# 17. RESPONSIVE LAW

## 17.1 Responsive behavior is mandatory

The application must work across relevant viewport sizes.

## 17.2 Responsive does not mean visually careless

Responsiveness must preserve:

- structure
- spacing discipline
- hierarchy
- readability
- design intent

## 17.3 No accidental overflow

Layout overflow,
clipping,
broken stacking,
and unstable spacing are unacceptable.

## 17.4 Fixed vs responsive behavior must be intentional

Every element must be evaluated correctly:

- what should scale
- what should wrap
- what should remain fixed
- what should adapt across breakpoints

No random “make it responsive somehow” implementation is acceptable.

## 17.5 Responsive scaling must align with rem-based layout policy

Responsive implementation must align with the CSS Unit Policy.

This means:

- avoid fixed pixel-driven layout sizing by default
- preserve scalable rhythm through rem-based utilities
- keep layout behavior accessible under zoom and scaling conditions

## 17.6 Fluid scaling must use clamp when appropriate

When an element truly requires fluid scaling between viewport sizes,
implementation must use `clamp()` instead of relying only on abrupt breakpoint jumps
or viewport-based sizing without boundaries.

Clamp must be applied intentionally
and only where it improves visual integrity and responsive proportionality.

---

# 18. COMPONENT LAW

## 18.1 Components must be purposeful

A component must have a clear reason to exist.

## 18.2 Presentation vs orchestration

Presentational components should remain as dumb as possible.

Container/orchestration logic should not be mixed carelessly
into pure display components.

## 18.3 Reusable component quality

Reusable components must be:

- generic enough to justify reuse
- stable in API
- typed clearly
- consistent with the design system

## 18.4 No oversized god components

Large components with mixed responsibilities
must be split when complexity becomes meaningful.

---

# 19. HOOK LAW

## 19.1 Custom hooks must encapsulate repeatable logic

Hooks should exist to encapsulate repeatable behavior
or feature logic,
not merely to cosmetically move code around.

## 19.2 Hooks must be predictable

A hook’s input,
output,
and side effects
must be explicit.

## 19.3 No business logic duplication

If multiple components repeat the same stateful logic,
extract it into a proper hook when justified.

---

# 20. ERROR HANDLING STANDARD

## 20.1 Error handling is mandatory

All remote operations must have defined error handling behavior.

## 20.2 Error UI pattern must be intentional

Error presentation may use:

- inline error message
- field-level form error
- toast notification
- retry UI
- page fallback UI
- section fallback UI

The chosen pattern must match the context.

## 20.3 Status-specific handling

The following status groups must not be treated identically
when the distinction matters:

- 400 validation/business errors
- 401 unauthenticated
- 403 unauthorized/forbidden
- 404 not found
- 409 conflict
- 500+ server failure

## 20.4 No swallowed errors

Errors must not disappear silently.

## 20.5 API-envelope awareness

Because backend failures use the response envelope:

- `success: false`
- `message`
- `data: null`

frontend error handling must not assume an `error` object exists.

---

# 21. LOADING / EMPTY / SKELETON LAW

Every data-driven UI should intentionally consider:

- loading state
- success state
- empty state
- error state

Do not design only for the happy path.

---

# 22. ACCESSIBILITY LAW

Minimum accessibility expectations are mandatory.

Requirements include:

- semantic HTML where appropriate
- accessible labels for forms and controls
- keyboard-friendly interaction
- correct button vs link usage
- meaningful alt text where needed
- visible focus states where interactive UI exists

Accessibility must not be treated as optional polish.

---

# 23. ANIMATION LAW

Framer Motion is allowed for motion behavior.

Rules:

- animation must support UX,
  not distract from it
- motion must not degrade usability
- animation must not compensate for weak layout or poor state logic
- avoid heavy or excessive motion on common flows

---

# 24. CLEAN CODE LAW

The following are mandatory:

- clear naming
- small focused functions
- low nesting where possible
- no dead code
- no unused exports left behind
- no meaningless comments
- no commented-out junk code
- no magic numbers when a named constant is more appropriate

Comments are allowed when they clarify real complexity.

---

# 25. PERFORMANCE LAW

Performance must be respected from the start.

Rules:

- avoid unnecessary re-renders
- avoid avoidable client components
- avoid unnecessary data duplication
- use memoization only when justified
- use pagination/infinite loading intentionally when required
- avoid rendering large lists carelessly
- do not fetch more than needed

Optimization must be meaningful,
not ritualistic.

---

# 26. SECURITY & DATA SAFETY LAW

Minimum safety requirements:

- do not trust client input blindly
- respect backend validation contracts
- do not leak sensitive data in UI logs or careless debugging
- environment variables must be handled correctly
- no hardcoded secrets
- no fake client-side security claims

This is frontend,
not magic.
It cannot pretend to secure what it does not control.

---

# 27. TESTABILITY LAW

Even if full test coverage is not introduced immediately,
architecture must remain test-friendly.

Rules:

- utility functions should be isolated and deterministic where possible
- data mapping logic should be testable
- validation logic should be testable
- complex feature logic should not be trapped inside impossible-to-test giant components

The codebase must not block future test adoption.

---

# 28. IMPORT / PATH LAW

## 28.1 Use clean import structure

Import structure must remain readable and consistent.

## 28.2 Prefer path aliases if configured

Use official project aliases consistently.

## 28.3 No chaotic cross-feature imports

A feature must not reach randomly
into another feature’s internals
unless officially exposed or justified.

---

# 29. NAMING LAW

Naming must be:

- explicit
- stable
- domain-relevant
- consistent

Bad naming patterns include:

- vague utility names
- meaningless abbreviations
- inconsistent plural/singular usage
- names that hide responsibility

## 29.1 Domain naming must match real system language

Naming must align with actual system terminology.

Examples:

- use `follow` when the backend/system uses follow behavior
- do not rename follow behavior into `friends`
  unless a formal domain translation layer is intentionally defined
- use `profile.me` / `profile.public` style distinctions clearly when useful
- distinguish `feed` from `explore posts`

No naming layer may misrepresent backend/domain reality carelessly.

---

# 30. IMPLEMENTATION DISCIPLINE

Before implementation,
the following must be true:

- requirement is understood
- feature boundary is clear
- source of truth is known
- placement is justified
- state ownership is correct
- styling approach is compliant
- API contract is known if backend is involved
- domain ownership is known if feature logic crosses domains

If any of the above is unclear,
the problem must be surfaced before coding.

---

# 31. CONFLICT REPORTING RULE

If any contradiction is found between:

- design and architecture
- API contract and UI expectation
- shared rules and feature need
- performance and visual demand
- this constitution and another governing document
- this constitution and session instructions

Then:

1. the contradiction must be reported explicitly
2. assumption-based implementation must stop
3. the constitution or source requirement must be revised first

---

# 32. FORBIDDEN PRACTICES

The following are explicitly forbidden:

- using Redux for server state
- fetching data directly in random UI components
- mixing UI state and remote entity cache carelessly
- bypassing the service/query layer
- adding unapproved libraries casually
- styling with random raw values without justification
- breaking folder boundaries for convenience
- implementing based on guesswork when contract is unclear
- creating shared abstractions without real justification
- overriding design intent arbitrarily
- using App Router as if it were Pages Router chaos
- turning everything into Client Components
- storing duplicated server entities in multiple state systems
- inventing “friends” architecture where the system contract is follow-based
- inventing upload endpoints or payload shapes not defined by the API contract
- using arbitrary pixel-based layout sizing by default where rem-based utilities should be used
- using viewport-based scaling without lower and upper bounds
  when clamp is required for fluid responsive behavior

---

# 33. DEFINITION OF TECHNICAL COMPLIANCE

An implementation is considered technically compliant
only if it satisfies all of the following:

- follows the locked stack
- respects folder and feature boundaries
- respects state ownership rules
- respects service/query/component data flow
- respects type safety rules
- respects design token and styling law
- respects CSS unit policy
- respects fluid responsive scaling policy
- respects responsive law
- respects clean code law
- respects error/loading/empty state expectations
- respects API contract accuracy
- introduces no unresolved architectural conflict

If one of these fails,
the implementation is not constitutionally complete.

---

# 34. FINAL GOVERNING PRINCIPLE

This project must be built with discipline.

Not with shortcuts.
Not with scattered assumptions.
Not with visual guesswork.
Not with state chaos.
Not with convenience-driven architecture.

Every implementation must be:

- architecture-compliant
- maintainable
- API-accurate
- design-token-compliant
- rem-scaling-compliant
- clamp-aware where required
- professionally defensible

If the architecture is not clear,
implementation must pause until clarity exists.

---

# 35. DESIGN TOKEN IMPLEMENTATION CONTRACT

The Design System Foundations document defines
the official visual tokens of the system.

However,
the implementation of those tokens inside the codebase
must follow a strict technical contract.

This prevents divergence between design and engineering.

Rules:

1. All visual tokens must originate from the official Design System Foundations document.

2. Raw tokens must be implemented as CSS variables inside the global styling layer.

Approved location example:

`src/app/globals.css`

3. CSS variable naming should follow a consistent token scheme.

Recommended prefixes:

- `--color-*`
- `--space-*`
- `--radius-*`
- `--font-*`

4. Components must not directly use raw design values.

Forbidden examples:

- `bg-[#F3F4F6]`
- `text-[#222]`
- `style={{ color: "#333" }}`

Correct approach:

Use semantic tokens or design-system utilities.

Examples:

- `bg-background`
- `text-foreground`

5. Tailwind arbitrary values must be minimized.

Arbitrary values such as:

- `w-[37px]`
- `top-[13px]`

may only be used when:

- design requires it
- no token or utility exists
- the value is not reusable

6. No parallel token system may be introduced.

All styling tokens must originate from
the official design token system.

---

# 36. SEMANTIC TOKEN LAYER

Between raw tokens and component usage,
a semantic layer must exist.

Raw tokens represent visual values.

Semantic tokens represent UI meaning.

Example mapping:

Raw token:
`--color-neutral-100`

Semantic token:
`--color-background`

Component usage:
`bg-background`

Example semantic tokens:

- `--color-background`
- `--color-foreground`
- `--color-card`
- `--color-border`
- `--color-muted`
- `--color-accent`
- `--color-primary`
- `--color-danger`

Components must prefer semantic tokens
rather than raw color tokens.

This ensures UI meaning remains stable
even if raw tokens change.

---

# 37. PHASE 1 LOCKED FEATURES

Development must proceed in controlled phases.

Phase 1 frontend development is restricted to the official roadmap.

Locked Phase 1 frontend features:

## Authentication

- Login
- Register

## Profile

- My profile
- Update profile
- View public user profile

## Timeline / Feed

- Feed display
- Explore post reading surface where needed
- Infinite scroll where required

## Post

- Create post
- View post detail

## Comments

- View comments
- Add comment

## Search

- Search users

The following features may exist in the backend
but are explicitly excluded from mandatory Phase 1 frontend interaction:

- likes interaction UI
- follow interaction UI
- bookmark management UI
- followers/following management UI
- notifications
- messaging
- groups
- stories
- advanced recommendation systems
- settings system

These may only be introduced in later phases
after Phase 1 architecture stabilizes
and the roadmap is officially revised.

---

# 38. GLOBAL PROVIDER BOUNDARY RULE

Global providers must be carefully controlled
to avoid turning the entire application tree
into client components.

Rules:

1. Root layout must remain a Server Component.

2. Client-side providers must be isolated
   inside a dedicated provider wrapper.

Approved example location:

`src/app/providers.tsx`

3. Only system-level providers may be global.

Examples:

- QueryClientProvider
- Redux Provider
- Theme Provider (if introduced formally)

4. Feature-specific providers must never wrap the entire application.

Forbidden example:

Wrapping the whole app with a PostProvider or CommentProvider.

Feature providers must exist only inside the feature that requires them.

---

# 39. DATA MODEL NAMING CONVENTION

To prevent confusion between backend responses,
domain models,
and UI-facing shapes,
the following naming convention is mandatory.

## 39.1 DTO (Data Transfer Object)

Represents raw backend response contracts.

Examples:

- `UserDto`
- `PostDto`
- `CommentDto`

DTO types must match backend API contracts exactly.

## 39.2 Domain Model

Represents the cleaned internal model
used by the application.

Examples:

- `User`
- `Post`
- `Comment`

Mapping from DTO → Domain Model must happen
in service or query layer.

## 39.3 View Model (optional)

Used only if UI requires a transformed data structure.

Examples:

- `PostViewModel`
- `UserProfileViewModel`

View models should exist only when necessary.

## 39.4 UI components should not consume DTO casually

UI components should consume domain/view models
unless there is a clear justified reason not to.

## 39.5 DTO naming must preserve backend field names

Fields such as the following must remain unchanged at DTO level:

- `avatarUrl`
- `createdAt`
- `likeCount`
- `commentCount`
- `likedByMe`
- `isFollowedByMe`

---

# 40. ARCHITECTURAL COMPLIANCE ENFORCEMENT

All implementations must pass the following checks:

- architecture compliance
- domain boundary compliance
- state boundary compliance
- folder structure compliance
- design token compliance
- CSS unit compliance
- fluid responsive scaling compliance
- type safety compliance
- API contract compliance
- roadmap phase compliance

Working code alone is insufficient.

Architecture correctness is mandatory.

If an implementation violates the constitution,
it must be refactored before merging.

---

# END OF DOCUMENT
